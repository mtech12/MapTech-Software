Install Instructions.

cd to qtserialpor directory.
mkdir serialport-build
cd serialport-build
qmake ../serialport.pro
make
sudo make install

Look at the MT500 code for correct includes and usage. It is important for
you to add the following line to the .pro file:
        CONFIG += serialport
